# hft_live Rust workspace

This workspace contains initial Rust crates to refactor the `hft_live` Python codebase.

- `hft_core`: domain models and `Exchange` trait
- `hft_engine`: simple reconciliation loop example (binary)
- `hft_exchange_mock`: a mock exchange adapter for testing

Run examples and tests per crate using `cargo` in the crate folder.
